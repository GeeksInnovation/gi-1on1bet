name: Run Tests

on:
  push:
    branches:
      - main
      - feat/**

  pull_request:
    branches:
      - main 

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Tests for All Projects
        run: mvn clean test
